# Зависимости для запуска тестов.
# Подтягивают все prod-зависимости через -r requirements.txt, поэтому
# достаточно одной команды:
#   pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.0.3
pytest-asyncio==0.25.2
psutil==6.1.1
