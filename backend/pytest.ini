[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: тесты длительностью > 5 секунд (большие отчёты, нагрузочные проверки)
    scenario: сценарные тесты с testcontainers (Docker)
    real_llm: тесты с реальным LLM API (до 100 строк, расход токенов)
